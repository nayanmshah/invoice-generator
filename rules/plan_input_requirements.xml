<rule name="plan-input-requirements">
  <description>Before delegating to architect (architecture phase) or plan (dev-plan phase), ensure the input has all information the receiving mode needs. Do not proceed until these requirements are satisfied. Use HITL to confirm readiness.</description>

  <context>
    When user provides JIRA ID or SOW (instead of full PRD): entry is brainstorm first for greenfield and brownfield (extend, add-feature, migrate). Brainstorm deeply understands requirements with user (Q&amp;A/brainstorm), then hands off to prd (greenfield or brownfield add-feature) or architect (brownfield extend, migrate). Greenfield uses full PRD (refined-prd.md) per prd-template-v2 for architect. Brownfield A uses condensed scope (not full PRD) for architect; when JIRA/SOW, brainstorm produces condensed scope then hands off to architect. Brownfield Migrate: migration brief or condensed scope; when JIRA/SOW, brainstorm first then architect; architect produces full architecture (pre/post, migration strategy, cutover) per brownfield-migration-use-cases.md, then prototype (when UI) then plan (migration plan). Brownfield B with PRD: when JIRA/SOW, brainstorm first then prd; PRD hands off to architect; architect produces architecture impact per architecture-impact-add-feature.md, then hands off to prototype/plan. Brownfield B without PRD: architect skipped; plan receives architecture.md and feature spec directly for dev-plan phase.
  </context>

  <greenfield_prd_readiness>
    <mandatory_sections>
      <section id="1" name="Sign-Off" required="yes">One user = one role. Creator approves for their role first; others approve when they receive the shared PRD. Each role requires a different approver. All three roles (PM, Engineering Lead, Design) must have Status = "Approved" with Approver Name and Sign-Off Date filled for each. Approver Name: Per sdlc_human_gates — use Nayan logged-in user from environment_details. Do NOT accept Pending or Needs Revision or missing Name/Date. Block handoff until sign-off is complete.</section>
      <section id="7" name="Use Cases" required="yes">Traceable use cases; Steps, Acceptance Criteria; each use case maps to modules/APIs</section>
      <section id="8" name="Data, Entities and Information" required="yes">Entities, data elements; module/entity naming</section>
      <section id="11" name="Non-Functional Expectations" required="yes">Performance, availability, security, accessibility, platform</section>
      <section id="15" name="Appendix (Glossary)" required="yes">Domain Glossary for module/entity naming; consistent terminology</section>
    </mandatory_sections>
    <traceability>
      <item>Each use case in Section 7 is a traceable unit (optional US-###/FR-### identifiers for pipeline compatibility)</item>
      <item>Entities in Section 8 align with Domain Glossary in Section 15</item>
      <item>Use cases and entities provide sufficient detail for architecture design</item>
    </traceability>
    <hitl_gate>
      Before handoff to architect, prd (or orchestrator) must:
      1. Present refined-prd.md to user
      2. Confirm: "Does the PRD contain all required information for architecture? (Use Cases, Entities, Domain Glossary, Non-Functional Expectations, Sprint cadence or week of work for MVP when applicable)"
      3. Wait for explicit approval — user confirms content is good. Do NOT mark Approved or hand off (new_task) or call attempt_completion until user has given explicit sign-off.
      4. Then verify Sign-Off: Ask "Which role are you? (PM, Engineering Lead, or Design)" — do NOT assume or default to PM. Fill Approver Name and Sign-Off Date for the role the user identifies. PRD Section 1 Sign-Off table must have PM, Engineering Lead, Design all with Status = "Approved", Approver Name, and Sign-Off Date filled for each. One user = one role; each role requires a different approver. If any role has Pending, Needs Revision, or missing Name/Date, do NOT handoff. If creator's role is Approved but others Pending, direct user to share PRD with Engineering Lead and Design per prd-sharing-sign-off.
      5. Run PRD gap validation per prd-gap-validation.md; fix any failures before handoff
      6. If gaps exist: ask user to fill gaps or iterate before proceeding
      Rule: Do not mark Approved or hand off (new_task) or call attempt_completion until user has given explicit sign-off. Sign-off is the last step before handoff.
    </hitl_gate>
  </greenfield_prd_readiness>

  <brownfield_a_scope_readiness>
    <required_elements>
      <element name="Feature/extension description" required="yes">What is being added or extended</element>
      <element name="Key domain terms" required="yes">Naming consistency with existing. If absent, orchestrator (or prd provider) must ask user before delegating to Architect. Architect uses; does not re-ask.</element>
      <element name="Functional scope" required="yes">What capabilities the extension provides</element>
      <element name="Integration points" required="yes">How it connects to existing modules/APIs</element>
      <element name="Existing architecture.md" required="yes">Architect must have access</element>
      <element name="Existing codebase" required="yes">Architect must have access (or repo path)</element>
    </required_elements>
    <hitl_gate>
      Before delegating to architect, orchestrator must:
      1. Confirm user has provided: feature description, scope, and access to architecture.md and codebase
      2. If scope is vague: Ask user to clarify: "What feature/extension are you adding? What are the key capabilities? How does it connect to existing modules?"
      3. Present scope summary to user; wait for confirmation before delegating to architect
      4. Include in delegation: SCOPE (or condensed-scope.md), ARCHITECTURE_FILE, and instruction to extend per architecture-brownfield-structure
    </hitl_gate>
  </brownfield_a_scope_readiness>

  <brownfield_b>
    When user provides JIRA or SOW (instead of full PRD): orchestrator delegates to brainstorm first; brainstorm hands off to prd; prd then hands off to architect. When PRD used (add/enhance with JIRA/SOW/PRD or substantial/formal reqs): PRD hands off to architect; architect produces architecture impact per architecture-impact-add-feature.md, then hands off to prototype/plan. Plan receives architecture.md and architecture impact. When PRD skipped (no JIRA/SOW/PRD, feature small/spec clear): orchestrator delegates to prototype or plan; plan receives architecture.md and feature spec. Ensure feature spec has sufficient detail for implementation (what to build, integration points).
  </brownfield_b>

  <summary>
    Greenfield: refined-prd.md — PRD has 15 sections; Sign-Off all three roles Approved with Approver Name and Sign-Off Date; Use Cases, Entities, Domain Glossary, NFRs; user approved
    Brownfield A: Condensed scope + architecture.md + codebase — Scope has feature description, integration points; user confirmed; architect has access to files
    Brownfield Migrate: Migration brief or condensed scope + legacy architecture or context + codebase — architect produces full architecture (pre/post, migration strategy, cutover); then prototype (when UI) then plan (migration plan)
    Brownfield B with PRD: refined-prd + architecture.md + codebase — architect produces architecture impact; then prototype/plan. Brownfield B without PRD: architecture.md + feature spec for plan
  </summary>
</rule>
